aiohttp>=3.8
# Upper bound is deliberate: _apply_codec_preference reorders the private
# transceiver._codecs list, which no compatibility promise covers.
aiortc>=1.7,<2
av>=12.0
numpy>=1.24
websockets>=12.0
# Optional for MJPEG streaming
Pillow>=10.0
pytest-asyncio>=0.23
# Needed by tools/tests/test_cfs_material_service.py, which executes the
# integration's service handler; the integration itself imports it via HA.
voluptuous
